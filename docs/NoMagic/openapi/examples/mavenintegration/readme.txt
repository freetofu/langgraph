This is MagicDraw Maven integration example. Example starts MagciDraw from maven task and generates all diagrams images
from project "execute/project.mdzip". Depends on "imagegenerator" example.

To run example:
1. Install maven.
2. Run "make.bat" to build ant install maven plugin.
3. Run "run.bat" to run example.